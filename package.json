{
  "name": "selfserve-egress",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute selfservegress --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260213.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.65.0"
  }
}
